#include <string>
#include <vector>

using namespace std;

int solution(string s) {
    int answer = 0;
    answer = stoi(s);
    /*
    for(char c : s){
        if (c=="+"){
            answer = 
        }
        else if (c=="-"){
            answer = 
        }
        else {answer = }
    }*/
    return answer;
}